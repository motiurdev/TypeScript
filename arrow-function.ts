// regular function 

function newFunc(num1: number, num2: number) {
    return num1 + num2;
}

// arrow function 
const arrowFunc = (num1: number, num2: number) => num1 + num2;

// multi line arrow function

const arrowFunction = (num1: number, num2: number): void => {
    console.log(num1 + num2);
}